include "main.h"

/**
* _isupper - check upper case 
* @c: a parameter to pass to the function
* Return: 0 or 1
*/
int _isupper(int c)
{
        if ((c >= 65) && (c <= 9)0)
	{
		return (1);
	}
	else{
		return (0);
	}
}
